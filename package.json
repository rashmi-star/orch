{
  "name": "music-intent-mcp",
  "version": "1.0.0",
  "type": "module",
  "description": "Orch MCP orchestrator — connects to music, YouTube, message MCPs and exposes prefixed tools",
  "main": "dist/index.js",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "predev": "node check-node.js",
    "postinstall": "mcp-use generate-types || exit 0",
    "build": "mcp-use build",
    "dev": "mcp-use dev",
    "start": "mcp-use start",
    "deploy": "mcp-use deploy"
  },
  "dependencies": {
    "mcp-use": "latest",
    "zod": "4.3.5"
  },
  "devDependencies": {
    "@types/node": "^25.3.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
